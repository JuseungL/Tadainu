<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tadainu" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="colorPrimary">@color/rcolor</item>
        <item name="colorSurfaceVariant">#FFD9D9D9</item>
    </style>
    <style name="Theme.Tadainu" parent="Base.Theme.Tadainu" />

    <style name="Theme.Tadainu.SignUp" parent="Base.Theme.Tadainu">
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Tadainu.ToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="titleTextColor">@color/black</item>
        <item name="backgroundColor">@color/white</item>
        <item name="elevation">3dp</item>
    </style>
</resources>
